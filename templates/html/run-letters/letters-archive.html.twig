<!DOCTYPE html>
<html lang="en">
<head>
    <title>Run Letters Archive - QT.run</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="#ffffff">
    <link rel="icon" type="image/jpeg" href="/assets/images/logo.jpg">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.4.1/dist/tailwind.min.css" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,300;0,400;1,300;1,400&family=Inter:wght@300;400&display=swap');

        body {
            font-family: 'Inter', sans-serif;
            background-color: #ffffff;
            color: #1a1a1a;
        }

        .serif {
            font-family: 'Crimson Pro', serif;
        }
    </style>
</head>
<body class="bg-white min-h-screen">

    <!-- Header -->
    <header class="border-b border-gray-200">
        <div class="max-w-4xl mx-auto px-6 py-8">
            <div class="flex items-center justify-between">
                <a href="/" class="flex items-center space-x-3 hover:opacity-70 transition-opacity">
                    <img src="/assets/images/logo.jpg" alt="QT.run" class="w-12 h-12 rounded-full object-cover">
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900">QT.run</h1>
                        <p class="text-sm text-gray-500">Run Letters Archive</p>
                    </div>
                </a>
                {% if athlete %}
                <a href="/admin" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">
                    Dashboard →
                </a>
                {% endif %}
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto px-6 py-12">

        {% if totalLetters > 0 %}
            <!-- Archive Header -->
            <div class="mb-12 text-center">
                <h2 class="text-4xl font-bold text-gray-900 mb-3">Run Letters</h2>
                <p class="text-lg text-gray-600">A collection of {{ totalLetters }} reflections on showing up</p>
            </div>

            <!-- Letters Grid -->
            <div class="space-y-8">
                {% for item in enrichedLetters %}
                    {% set letter = item.letter %}
                    {% set activity = item.activity %}

                    <article class="border border-gray-200 rounded-lg p-8 hover:shadow-lg transition-shadow duration-300">
                        <!-- Date and Meta -->
                        <div class="flex items-center justify-between mb-6 text-sm text-gray-500">
                            <time datetime="{{ activity.getStartDate().format('Y-m-d') }}">
                                {{ activity.getStartDate().formatLocalized('%B %e, %Y') }}
                            </time>
                            <div class="flex items-center space-x-4">
                                <span>{{ activity.getDistanceInKilometer().getFormatted() }}</span>
                                <span>•</span>
                                <span>{{ activity.getFormattedMovingTime() }}</span>
                            </div>
                        </div>

                        <!-- Letter Content -->
                        <blockquote class="serif text-xl leading-relaxed text-gray-800 italic mb-6">
                            "{{ letter.getLetterText() }}"
                        </blockquote>

                        <!-- Footer -->
                        <div class="flex items-center justify-between pt-6 border-t border-gray-100">
                            <div class="text-xs text-gray-400">
                                {% if letter.wasEdited() %}
                                    <span class="flex items-center gap-x-1">
                                        <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m14.304 4.844 2.852 2.852M7 7H4a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h11a1 1 0 0 0 1-1v-4.5m2.409-9.91a2.017 2.017 0 0 1 0 2.853l-6.844 6.844L8 14l.713-3.565 6.844-6.844a2.015 2.015 0 0 1 2.852 0Z"/>
                                        </svg>
                                        Edited
                                    </span>
                                {% else %}
                                    <span class="flex items-center gap-x-1">
                                        <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.813 15.904 9 18.75l2.846-.81-2.033-2.036zm5.107-2.997-2.033-2.036L18.309 5.45l2.033 2.036-5.422 5.421z"/>
                                        </svg>
                                        AI Generated
                                    </span>
                                {% endif %}
                            </div>
                            {% if letter.getShareToken() %}
                                <a href="/letter/{{ letter.getShareToken() }}"
                                   class="text-sm text-gray-600 hover:text-gray-900 transition-colors flex items-center gap-x-1">
                                    View letter
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                    </svg>
                                </a>
                            {% endif %}
                        </div>
                    </article>
                {% endfor %}
            </div>

        {% else %}
            <!-- Empty State -->
            <div class="text-center py-20">
                <div class="mb-8">
                    <svg class="mx-auto h-24 w-24 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                    </svg>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-3">No Public Letters Yet</h3>
                <p class="text-gray-600 max-w-md mx-auto">
                    The archive will appear here once letters are shared publicly.
                </p>
            </div>
        {% endif %}

    </main>

    <!-- Footer -->
    <footer class="border-t border-gray-200 mt-20">
        <div class="max-w-4xl mx-auto px-6 py-8 text-center text-sm text-gray-500">
            <p>QT.run - Every run tells a story</p>
        </div>
    </footer>

</body>
</html>
